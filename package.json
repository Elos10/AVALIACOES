{
  "name": "avd-diagnostico-escolar",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Plataforma AVD para GitHub Pages com Supabase.",
  "scripts": {
    "check": "node --check supabase-init.js && node --check public/app.js && node scripts/check-supabase-config.mjs"
  },
  "engines": {
    "node": "20"
  }
}
